import stringInterpolation from '../../examples/files/syntax/stringInterpolation.ts'

We can use variables within strings — `.toString` is called automatically to convert each variable to a string.

<Example code={stringInterpolation} />
