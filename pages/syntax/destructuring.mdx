import destructuringObjects from '../../examples/files/syntax/destructuringObjects.ts'
import destructuringArrays from '../../examples/files/syntax/destructuringArrays.ts'
import destructuringFunctionArguments from '../../examples/files/syntax/destructuringFunctionArguments.ts'
import destructuringAdvanced from '../../examples/files/syntax/destructuringAdvanced.ts'

# Destructuring

We can extract multiple values from an object or array and assign them to variables simultaneously.

---

## Destructuring objects

<Example code={destructuringObjects} />

---

## Destructuring arrays

<Example code={destructuringArrays} />

---

## Destructuring function arguments

<Example code={destructuringFunctionArguments} />

---

## Advanced destructuring

We can also nest and rename destructured variables and supply default values.

The rest syntax `...` collects remaining values. <Details>Rest is roughly the inverse of spread.</Details>

<Example code={destructuringAdvanced} />