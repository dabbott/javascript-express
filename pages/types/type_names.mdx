import typeofExample from '../../examples/files/types/typeof.ts'

## Type names

We can find the type name of a value _at runtime_ using the `typeof` keyword:

<Example   
  title={"Literal values"}
  code={typeofExample}
/>
